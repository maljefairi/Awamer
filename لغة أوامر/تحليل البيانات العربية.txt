أكتب مباشرة الكود البرمجي المقترح بشكل صحيح وجملة صحيحة يمكن قرائتها وفهمها من قبل الجميع باللغة العربية الفصحى مع ذكر مثال لطريقة تطبيق هذا الأمر البرمجي 

---
## 📜 تحليل البيانات العربية

لإنشاء لغة برمجة باللغة العربية الفصحى تستخدم الجمل الصحيحة لتحليل البيانات العربية، يمكننا استخدام بنية باللغة العربية تشبه هيكل الكود البرمجي في لغات البرمجة الأخرى. تحليل البيانات العربية يستدعي وجود كود قراءة الملفات وكود لمعالجة البيانات والتحليل.

### 📖 فتح وقراءة ملف البيانات 

فلنفترض أن لدينا الأمر البرمجي العربي `افتح` لفتح الملفات.

```arabic
افتح ملف البيانات.csv كـ ملف
```
يعمل الكود المقترح على فتح ملف بيانات بصيغة الـ csv وتخزين البيانات في متغير يدعى `ملف`.

### 📖 المعالجة والتحليل 

ثم يمكننا استخدام الأمر `للكل` لتنفيذ تعليمات على كل سطر في الملف والأمر `اذا` للقيام بمعالجة بسيطة.

```arabic
للكل سطر في ملف:
    اذا سطر[0] == 'الأحد':
           طباعة(سطر)
```
هذا الكود يقوم بطباعة كل السطور في الملف التي تحتوي على كلمة 'الأحد' في العمود الأول من كل سطر.
            
---
