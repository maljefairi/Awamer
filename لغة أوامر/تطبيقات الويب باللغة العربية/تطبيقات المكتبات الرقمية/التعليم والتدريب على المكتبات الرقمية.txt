أكتب مباشرة الكود البرمجي المقترح بشكل صحيح وجملة صحيحة يمكن قرائتها وفهمها من قبل الجميع باللغة العربية الفصحى مع ذكر مثال لطريقة تطبيق هذا الأمر البرمجي 

عذرا، ليس ممكنا كتابة كود في لغة برمجة تكون باللغة العربية الفصحى حاليا لأنه لا توجد لغة برمجة من هذا النوع. ولكن فيما يلي مثال عام على كيفية كتابة وظيفة بسيطة باستخدام بايثون الذي يدعم خاصية Unicode:

```python
def التسجيل(اسم_المستخدم, كلمة_السر):
    if تحقق_من_المستخدم(اسم_المستخدم, كلمة_السر):
        print('تم التسجيل بنجاح')
    else:
        print('الرجاء كتابة اسم المستخدم وكلمة السر بشكل صحيح')

def تحقق_من_المستخدم(اسم_المستخدم, كلمة_السر):
    # هنا يمكنك الاتصال بقاعدة البيانات للتحقق من صحة اسم المستخدم وكلمة السر
    return True
```

لاحظ أن الكود الموجود أعلاه سيعمل فقط إذا كان لديك واجهة برمجة التطبيقات(API) للتواصل مع قاعدة البيانات للتحقق من صحة اسم المستخدم وكلمة السر. للأسف، لغة البرمجة الأكثر شيوعا لا تدعم كتابة الأكواد بالكامل باللغة العربية.